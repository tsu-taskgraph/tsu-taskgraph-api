# tsu-taskgraph-api
The central orchestration engine for TaskGraph, a semantic DAG-based project management system. It manages the project lifecycle, task dependencies, and automated unlocks via a Spring Boot backend. Features include topological sorting, integrity checks to prevent cycles, and seamless integration with the AI Bridge.
